[project]
name = "koa"
version = "0.1.0"
description = "Koa — a free, step-by-step ukulele learning web app"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]
